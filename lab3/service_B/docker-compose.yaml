services:
  service_b:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: service_b
    env_file:
      - ../.env
    networks:
      - lab3-net

networks:
  lab3-net:
    external: true
